name: flutter_livreur
description: Application livreur Hill — dashboard + vente rapide
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter: { sdk: flutter }
  flutter_localizations: { sdk: flutter }
  http: ^1.2.0
  shared_preferences: ^2.2.2
  intl: ^0.20.2

dev_dependencies:
  flutter_test: { sdk: flutter }
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/
